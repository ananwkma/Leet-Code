/*
 * https://leetcode.com/problems/reach-a-number/
 * @param {number} target
 * @return {number}
 */
var reachNumber = function(target) {
    
};

const test = "1"
console.log('Expected: 1' + '\n' + 'Output: ' + reachNumber(test))